# Anthropic API key used by /api/report to generate AI reports.
# Without this (or if the key is invalid / the model times out), the route
# falls back to the deterministic report generator in src/lib/generate-report.ts.
# Get a key at https://console.anthropic.com/ and copy it into .env.local.
ANTHROPIC_API_KEY=
